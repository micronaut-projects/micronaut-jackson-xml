pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id("io.micronaut.build.shared.settings") version "6.0.1"
}

rootProject.name = "jackson-xml-parent"

include 'jackson-xml'
